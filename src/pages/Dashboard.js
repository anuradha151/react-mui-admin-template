import React from 'react';
import { Typography } from '@mui/material';

function Dashboard() {
  return (
    <div>
        <Typography variant="h4">Dashboard</Typography>
        <Typography>View your dashboard here.</Typography>
    </div>
  );
}

export default Dashboard;
